<% if obj.errors.any? %>
  <div class="field_with_errors">
    <h2>The article can't be saved</h2>
    <ul>
      <% obj.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>